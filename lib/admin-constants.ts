/** Cookie name for admin session (Edge-safe, no Node built-ins). */
export const ADMIN_SESSION_COOKIE = "nagata_admin_session";

export const ADMIN_SESSION_FALLBACK_SECRET = "nagata-juku-default-secret";
